/*package com.example.upeapp12.Activities

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import androidx.recyclerview.widget.RecyclerView
import com.example.upeapp12.R
import com.example.upeapp12.adapters.Taskadapter
import com.example.upeapp12.models.Task

class blocos_mainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_blocos_main)

        val recyclerView: RecyclerView = findViewById(R.id.recyclerView_blocos)
        recyclerView.layoutManager = LinearLayoutManager(this)
        val adapter = Taskadapter(Task(this)) // Substitua generateDummyTasks() pela sua lista de tarefas
        recyclerView.adapter = adapter
    }
}
*/
